QR Code Generator

# install dependencies  (npm install )       
# run your QR generator ( node index.js )

A simple QR Code Generator built with Node.js that uses terminal input to create QR codes from text or URLs and saves them as image files.
